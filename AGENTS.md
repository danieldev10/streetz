# Streetz Codex Context

- Canonical project path: `~/Code/streetz` (`/Users/daniel/Code/streetz`).
- Do not use `/Users/daniel/Desktop/streetz` for this project; older Codex sessions may reference that stale path.
- This is the Streetz/Crushclub monorepo: `apps/web` is the Next.js app and `apps/api` is the NestJS/Prisma API.
- When working in `apps/web`, also follow `apps/web/AGENTS.md`.
